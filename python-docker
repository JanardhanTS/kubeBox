mkdir -p /home/jana
cd /home/jana
#for apt use apt-install
sudo apt-get install python3-venv
#python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install Flask
python3 -m pip freeze > requirements.txt
touch app.py
